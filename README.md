# MyApp

Minimal SwiftUI iOS app. Standalone — unrelated to the macOS app next door.

    open MyApp.xcodeproj      # or:
    xcodebuild -scheme MyApp -sdk iphonesimulator build

Needs the iOS platform installed in Xcode (Settings -> Platforms).
